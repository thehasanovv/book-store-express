// $(document).ready(function () {});

// $('.catalog-card').on('click', function (e) {
//   const clicked = e.target.closest('.read-more-btn');
//   if (!clicked) return;

//   const id = $(this).data('id');
//   // window.location = `/catalog/${id}`;
//   // clicked.href = `/catalog/${id}`;
//   console.log(id);
//   console.log(clicked);
// });
